version: '3.8'

services:
  web:
    build: .
    ports:
      - '4173:80'
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
